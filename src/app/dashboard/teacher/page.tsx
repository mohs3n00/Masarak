import { DashboardContainer } from "@/shared/layouts/Containers";
import { SectionTitle } from "@/shared/components/molecules/SectionTitle";

export default function Page() {
  return (
    <DashboardContainer>
      <SectionTitle title="/dashboard/teacher Route" subtitle="Placeholder for dashboard content" />
    </DashboardContainer>
  );
}